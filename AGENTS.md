# AGENTS.md — 开发工作区说明

本文档为后续 AI 开发助手提供项目工作区使用规范。

---

## 文件夹结构及用途

| 文件夹 | 用途 | 规则 |
|--------|------|------|
| `src/` | 所有代码文件 | 独立的代码区，不引用外部文件；代码自包含、可独立构建运行 |
| `docs/` | 产品文档 | 存放 PRD、需求迭代记录、产品关键决策；所有文档类产出统一放这里 |
| `assets/design/` | 设计素材 | 效果图、UI 参考图、设计稿 |
| `assets/bug/` | 测试报错截图 | 测试过程中产生的报错截图、异常日志截图 |
| `assets/reference/` | 参考与灵感 | 竞品截图、灵感收集、技术参考图 |
| `notes/` | 学习笔记 | 开发中踩过的坑、技术方案总结、经验记录 |

---

## 开发规则

1. **代码隔离** — `src/` 是独立的代码目录，内部代码不得通过相对路径引用外部文件（如 `../assets`、`../docs`）。所有资源引用使用 src 内部路径或构建系统配置的别名。

2. **文档归位** — 所有产品文档、设计文档、决策记录一律放入 `docs/`，不在根目录散落 `.md` 文件（`AGENTS.md` 和 `README.md` 除外）。

3. **素材分类** — 设计图 / Bug 截图 / 参考图分属不同子目录，不要混放。

4. **笔记独立** — `notes/` 下的内容为开发者个人/团队经验记录，不参与构建，不参与部署。

5. **根目录整洁** — 除项目配置文件（`package.json`、`tsconfig.json` 等）和本说明文件外，不在根目录新增散落文件。

6. **优先复用** — 新增功能前先检查 `src/` 和 `docs/` 中是否已有相关代码或文档，避免重复造轮子。

7. **变更记录** — 需求迭代和重要决策的变更记录放入 `docs/`，并使用清晰的命名（如 `PRD-v2.md`、`decision-auth-strategy.md`）。
