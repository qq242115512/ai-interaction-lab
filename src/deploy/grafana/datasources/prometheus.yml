# Auto-provisioned Prometheus datasource for Grafana
# Grafana runs in Docker with --network host → can reach backend on localhost:8080
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://localhost:8080
    isDefault: true
    editable: false
